export interface Address {
    id?: number,
    userid?: number,
    street: string,
    streetNumber: string,
    door?: string,
    stair?: string,
    state: string,
    postcode: number,
    location: string,
}
